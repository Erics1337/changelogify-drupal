{#
/**
 * @file
 * Template for a single release detail page.
 */
#}
<article class="changelogify-release">
  <header>
    <div class="release-meta">
      <time datetime="{{ date_iso }}">
        {{ date }}
      </time>
      {% if version %}
        <span class="version">{{ 'Version @version'|t({'@version': version}) }}</span>
      {% endif %}
    </div>
  </header>

  <div class="release-sections">
    {% for key, section in sections %}
      <section class="release-section release-section--{{ key }}">
        <h2>{{ section.label }}</h2>
        <ul>
          {% for item in section.items %}
            <li>{{ item.text }}</li>
          {% endfor %}
        </ul>
      </section>
    {% endfor %}
  </div>
</article>
